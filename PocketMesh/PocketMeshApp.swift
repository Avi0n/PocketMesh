import SwiftUI
import PocketMeshKit

@main
struct PocketMeshApp: App {
    @State private var appState = AppState()

    var body: some Scene {
        WindowGroup {
            ContentView()
                .environment(appState)
        }
    }
}
